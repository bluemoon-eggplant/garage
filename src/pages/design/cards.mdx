---
layout: '../../layouts/Page.astro'
title: Cards
---

import PostCard from '../../components/PostCard.astro';
import PostCardMore from '../../components/PostCardMore.astro';
import PostCardSmall from '../../components/PostCardSmall.astro';

import GarageCard from '../../components/GarageCard.astro';

import { getAllPostsWithReadingTime } from '../../modules/post/common';
import { getAllGarageItems } from '../../modules/garage';

back to [Index](/design)

export const posts = await getAllPostsWithReadingTime();
export const post = posts[0];
export const threePosts = posts.slice(0, 3);

export const postWithoutImage = {
  ...post,
  data: {
    ...post.data,
    noHero: true,
  },
};

export const garageItems = await getAllGarageItems();
export const garageItem = garageItems[0];
export const threeGarageItems = garageItems.slice(0, 3);

## PostCard

<div class="not-prose">
  <PostCard post={post} />
</div>

---

## PostCard without Image

<div class="not-prose">
  <PostCard post={postWithoutImage} />
</div>

---

## PostCardMore

<div class="not-prose flex gap-4">
  {threePosts.map((post) => (
    <PostCardMore post={post} />
  ))}
</div>

---

## PostCardSmall

<div class="not-prose">
  {threePosts.map((post) => (
    <PostCardSmall post={post} />
  ))}
</div>

---

## GarageCard

<div class="not-prose flex gap-4">
  {threeGarageItems.map((item) => (
    <GarageCard item={item} />
  ))}
</div>
