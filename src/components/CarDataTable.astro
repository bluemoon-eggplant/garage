---
import RecordDataTable from '@/components/react/RecordDataTable';

import type { MaintenanceTableRow } from '@/types/record';

interface Props {
  data: MaintenanceTableRow[];
  category: string;
  caption?: string;
}

const { data, category, caption } = Astro.props;
const locale = Astro.currentLocale ?? 'ja';
---

<RecordDataTable
  client:load
  data={data}
  category={category}
  caption={caption}
  locale={locale}
/>
